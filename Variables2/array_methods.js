// ITERATE OVER
// 1. for loop 
// 2. for of (iterate index)
// 2. for in (iterate value)
// 3. array.forEach(element => { });

// CREATE ARRAY
//  []
// Array()

// ADD ELEMENT 
// push (at the end)
// unshift ( to the beginning )
// splice  (from start position)

// REMOVE ELEMENT -> return removed
// pop ( remove the last)
// shift ( remove the first )
// splice  (remove or replace existing elements)

// COPY ARRAY
// slice (shallow copy of an array or a portion of it)

// FIND ELEMENT
// includes 
// indexOf (first index of the element )
// lastIndexOf ( last index of the element)

// Callback params ->  element, index, array
// findIndex (first index of the condition ) -1
// find (first value of the condition) undefined 

// ADVANCED MANIPULATIONS 
// map ( create array based on result )
// filter ( create array filter conditions )
// reduce (single output based on reducer operation)
// flat  (create array sub-array elements spec depth)
// every (true all element (cond) )
// some ( true some element (cond))


// ARRAY UTILS 
// array.concat(array)
// toString() 
// array.join(separator|optional)
// array.sort () -> (a,b) => b-a (descending) 
// array.reverse()
// spread ['bien', ...['hola', 'que tal']]
// spread f1(p1, p2, p3) f1(...[p1, p2, p3, p4])
// rest f1(...rest) f1(p1, p2, p3) -> rest:[p1, p2, p3]


